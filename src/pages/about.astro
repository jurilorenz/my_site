---
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout 
  title="Juri Lorenz's About" 
  seoTitle="About Juri Lorenz, Web Developer"
  seoDesc={"Hi there! My name is Juri Lorenz and I'm a web developer " +
    "with over 5 years of experience in the industry."}
>
  <!-- Background with gradient -->
  <div class="min-h-screen bg-gradient-to-b from-[#090a1a] to-[#290065] text-white text-center flex flex-col items-center justify-center">
    
<!-- Title -->
<h1 class="text-center text-4xl md:text-6xl font-bold text-gray-100 drop-shadow-lg my-16 md:my-24">
  About Me
</h1>

<div class="flex flex-col items-center md:flex-row md:justify-center md:items-center md:space-x-32 mt-12">
  <!-- Profile Image with Gradient Border and Shadow -->
  <div class="rounded-full p-1 bg-gradient-to-r from-purple-400 to-blue-500 shadow-md drop-shadow-[-35px_0px_35px_rgba(75,0,130,0.6)] profile-container">
    <div class="rounded-full bg-[#090a1a] p-0">
      <img src="/images/about.png" class="rounded-full w-48 h-48 md:w-56 md:h-56 object-cover" alt="Juri Lorenz" />
    </div>
  </div>

  <!-- Motto Text Beside Image -->
  <div class="text-center md:text-left md:pl-12 mt-6 md:mt-0">
    <p class="text-lg md:text-xl font-medium text-gray-300">
      <span>Web Development</span><br />
      <span>Munich</span><br />
    </p>

    <!-- Main Motto -->
    <p class="mt-4 text-3xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-500">
      <span>Achieving</span><br />
      <span>Excellence</span><br />
      <span>with Joy</span>
    </p>
  </div>
</div>
   
<!-- Content Section -->
<div class="w-11/12 md:w-10/12 lg:w-8/12 mx-auto text-lg md:text-xl text-gray-300 leading-relaxed mt-20 md:mt-32 flex flex-col-reverse md:flex-row items-center gap-16 md:gap-32">
  
<!-- Phone Image Section for Desktop on Left -->
<div class="w-full md:w-5/12 flex justify-center md:justify-end mb-12 md:mb-0 rounded shadow-md drop-shadow-[-35px_0px_35px_rgba(75,0,130,0.6)] phone-container">
  <img src="/images/about-phone1.png" alt="Phone Screenshot" class="w-80 md:w-auto md:max-h-[60vh] object-contain rounded-lg">
</div>
  
  <!-- Text Section for Desktop on Right -->
  <div class="w-full md:w-7/12 text-center md:text-left">
    <p class="text-xl md:text-2xl mt-4">
      Hi, I’m <span class="bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-blue-500">Juri Lorenz</span>, an experienced app and web developer based in Munich, Germany, with a background in Computer Science and Marketing. I specialize in creating <span class="underline-animation">conversion-focused, mobile-first web applications</span> that deliver fast, seamless experiences across all devices.
    </p>
    
    <p class="text-xl md:text-2xl mt-8">
      With over <span class="text-gradient">5 years of professional experience</span>, I’ve developed a strong foundation in Android and iOS development, WordPress, Astro, React, and Next.js. My experience in mobile app development ensures that every website I build is truly mobile-first, offering an optimal user experience from smartphones to desktops.
    </p>
  </div>
</div>

<!-- What I Offer Section with Cards -->
<h2 class="text-center text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-500 mt-36 mb-24">
  What I Offer
</h2>

<div class="grid grid-cols-1 md:grid-cols-2 gap-12 px-6 md:px-0 max-w-6xl mx-auto">
  <!-- Card 1: Web Development -->
  <div class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)] hover:shadow-2xl transition-transform duration-500 hover:scale-105">
    <div class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"></div>
    <h4 class="text-3xl font-bold text-white mb-6">Web Development</h4>
    <p class="text-lg text-gray-300 leading-relaxed">
      Creating web applications that prioritize <strong>speed</strong>, <strong>usability</strong>, and <strong>conversions</strong> on mobile devices.
    </p>
  </div>

  <!-- Card 2: AI Integration -->
  <div class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)] hover:shadow-2xl transition-transform duration-500 hover:scale-105">
    <div class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"></div>
    <h4 class="text-3xl font-bold text-white mb-6">AI Integration</h4>
    <p class="text-lg text-gray-300 leading-relaxed">
      Adding AI solutions like <strong>chatbots</strong> or <strong>automation</strong> to enhance user experiences and business processes.
    </p>
  </div>

  <!-- Card 3: SEO Optimization -->
  <div class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 shadow-[0px_0px_35px_rgba(147,112,219,0.6)] hover:shadow-2xl transition-transform duration-500 hover:scale-105">
    <div class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"></div>
    <h4 class="text-3xl font-bold text-white mb-6">SEO Optimization</h4>
    <p class="text-lg text-gray-300 leading-relaxed">
      Ensuring your website ranks well and attracts organic traffic with <strong>SEO best practices</strong>.
    </p>
  </div>

  <!-- Card 4: Mobile Development -->
  <div class="relative bg-[#1b1b33] p-8 rounded-xl border border-gray-700 hover:shadow-2xl transition-transform duration-500 hover:scale-105 shadow-[0px_0px_35px_rgba(147,112,219,0.6)]">
    <div class="absolute top-0 left-0 bg-gradient-to-r from-purple-400 to-blue-500 w-full h-2 rounded-t-xl"></div>
    <h4 class="text-3xl font-bold text-white mb-6">Mobile Development</h4>
    <p class="text-lg text-gray-300 leading-relaxed">
      Developing powerful and intuitive mobile applications for both <strong>iOS and Android</strong> platforms.
    </p>
  </div>
</div>

<!-- Why Work With Me Section -->
<h2 class="text-center text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-500 mt-36 mb-24">
  Why Work With Me?
</h2>

<div class="w-11/12 md:w-10/12 lg:w-8/12 mx-auto flex flex-col md:flex-row items-center gap-16 md:gap-32 mb-24">

  <!-- Phone Image Section for Desktop on Left -->
  <div class="w-full md:w-5/12 flex justify-center md:justify-start mb-12 md:mb-0 rounded shadow-md drop-shadow-[-35px_0px_35px_rgba(75,0,130,0.6)] phone-container-second">
    <img src="/images/about-phone2.png" alt="Phone Screenshot" class="w-80 md:w-auto md:max-h-[60vh] object-contain rounded-lg">
  </div>

  <!-- Text Section for Desktop on Right -->
  <div class="w-full md:w-7/12 text-center md:text-left">
    <p class="text-xl md:text-2xl mt-4">
      Having worked on projects in diverse industries like telemedicine, e-commerce, and startups, I combine technical expertise with a strong marketing mindset. 
      My goal is to create <span class="underline-animation">fast, user-friendly, and scalable web applications</span> that drive business results. 
      Whether you need a sleek corporate website or a dynamic web application, I’m here to make it happen.
    </p>

    <!-- Call to Action -->
    <p class="text-xl md:text-2xl mt-8">
      <span class="bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-blue-500">
        Feel free to get in touch with me to discuss how we can make your next project a success!
      </span>
    </p>

    <!-- Buttons Section -->
    <div class="mt-8 flex flex-col md:flex-row gap-6 justify-start md:justify-start">
      <!-- Primary Button -->
      <a href="/projects" class="bg-white text-[#090a1a] font-bold py-3 px-8 rounded-full shadow-lg hover:bg-gray-200 transition-all text-center w-full max-w-xs md:max-w-none md:w-auto">
        Discover Projects
      </a>

      <!-- Secondary Button -->
      <a href="/contact" class="bg-[#1f1235] text-white font-bold py-3 px-8 rounded-full border-2 border-[#6d87d1] shadow-lg hover:bg-[#6d87d1] hover:text-white transition-all text-center w-full max-w-xs md:max-w-none md:w-auto">
        Contact Me
      </a>
    </div>
  </div>
</div>
     
  </div>
</MainLayout>

<style>
  .text-gradient {
    background: linear-gradient(to right, #9c27b0, #03a9f4);
    -webkit-background-clip: text;
    color: transparent;
  }

  .underline-animation {
    display: inline-block;
    background-image: linear-gradient(120deg, #03a9f4, #9c27b0);
    background-repeat: no-repeat;
    background-size: 100% 3px;
    background-position: 0 88%;
    transition: background-size 0.3s ease-in-out;
  }

  .underline-animation:hover {
    background-size: 100% 100%;
  }

  .drop-shadow-lg {
    filter: drop-shadow(0 8px 16px rgba(0, 0, 0, 0.2));
  }

  .border-gradient-to-r {
    border-image-slice: 1;
    border-image-source: linear-gradient(to right, #9c27b0, #03a9f4);
  }

    /* Container for the entire image, gradient, and shadow */
    .profile-container {
    transform: translateX(-150%); /* Start off-screen to the left */
    opacity: 0; /* Initially hidden */
    transition: transform 1.5s ease-out, opacity 1.5s ease-out;
  }

  /* Trigger the animation when the page loads */
  .profile-container {
    animation: flyInFromLeft 1.5s forwards;
  }

  /* Keyframes for the fly-in effect */
  @keyframes flyInFromLeft {
    0% {
      transform: translateX(-150%);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }

  /* Container for the entire image and shadow */
  .phone-container {
    transform: translateX(-150%); /* Start off-screen to the left */
    opacity: 0; /* Initially hidden */
    transition: transform 1.5s ease-out, opacity 1.5s ease-out; /* Remove delay for mobile */
  }

  /* Fly in effect */
  .fly-in {
    transform: translateX(0);
    opacity: 1;
  }

  /* Keyframes for desktop (initial load) */
  @media (min-width: 768px) {
    .phone-container {
      animation: flyInFromLeftPhone 1.5s forwards 0.5s; /* Keep delay on desktop */
    }
  }

  /* Keyframes for mobile (on scroll) */
  @keyframes flyInFromLeftPhone {
    0% {
      transform: translateX(-150%);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }

  /* Initial state for the second phone container */
.phone-container-second {
  transform: translateX(-150%); /* Positioned off-screen to the left */
  opacity: 0; /* Initially hidden */
  transition: transform 1.5s ease-out, opacity 1.5s ease-out;
}

/* Fly-in effect when in view */
.fly-in-second {
  transform: translateX(0); /* Return to original position */
  opacity: 1; /* Fully visible */
}

</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const phoneImage = document.querySelector('.phone-container');

    // Detect if the element is in view for mobile
    function isInViewPort(element) {
      const rect = element.getBoundingClientRect();
      return (
        rect.top <= (window.innerHeight || document.documentElement.clientHeight) &&
        rect.bottom >= 0
      );
    }

    // Add event listener for scroll to trigger the animation
    window.addEventListener('scroll', function() {
      if (window.innerWidth < 768) { // Only apply for mobile view
        if (isInViewPort(phoneImage)) {
          phoneImage.classList.add('fly-in');
        }
      }
    });
  });

  document.addEventListener('DOMContentLoaded', function() {
  const secondPhoneImage = document.querySelector('.phone-container-second');

  // Function to check if the element is in the viewport
  function isInViewPort(element) {
    const rect = element.getBoundingClientRect();
    return (
      rect.top <= (window.innerHeight || document.documentElement.clientHeight) &&
      rect.bottom >= 0
    );
  }

  // Function to add the fly-in class when in view
  function handleScroll() {
    if (isInViewPort(secondPhoneImage)) {
      secondPhoneImage.classList.add('fly-in-second');
    }
  }

  // Add scroll event listener
  window.addEventListener('scroll', handleScroll);

  // Run on page load in case the element is already in view
  handleScroll();
});
</script>